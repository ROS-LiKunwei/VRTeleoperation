# --- Unity project: do NOT put core folders in LFS ---
# Keep these as normal Git text/binary
Assets/* text merge=unityyamlmerge
ProjectSettings/* text merge=unityyamlmerge
Packages/* text merge=unityyamlmerge

# --- Unity meta files should always be text ---
*.meta text

# --- Optional: line endings normalization ---
* text=auto

# --- OPTIONAL: if you want LFS for large binaries, uncomment these ---
#*.psd filter=lfs diff=lfs merge=lfs -text
#*.fbx filter=lfs diff=lfs merge=lfs -text
#*.png filter=lfs diff=lfs merge=lfs -text
#*.jpg filter=lfs diff=lfs merge=lfs -text
#*.mp4 filter=lfs diff=lfs merge=lfs -text
#*.wav filter=lfs diff=lfs merge=lfs -text
